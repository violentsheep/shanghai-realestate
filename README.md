# 上海房地产数据看板

每日自动采集上海官方房地产交易数据，可视化展示。

🌐 **在线预览**：https://afw6pe105p.space.minimaxi.com

## 数据来源

**上海市房地产交易中心** — [网上房地产 fangdi.com.cn](https://www.fangdi.com.cn)

| 数据类型 | 说明 | 更新频率 |
|---|---|---|
| 二手房昨日成交套数、面积 | 网签数据（T+1） | 每日06:00 |
| 一手房今日成交套数、面积 | 当日累计 | 实时 |
| 二手房挂牌套数 | 全市出售挂牌总量 | 实时 |

> robots.txt：不存在（404），无爬取限制规则。采集间隔 ≥5 秒，合规操作。

## 技术架构

```
GitHub Actions (每日 08:00)
  ↓
Python + Playwright 截图
  ↓
Gemini Vision OCR 解析数字
  ↓
data/history/data.json 追加写入 → git commit
  ↓
Vite 构建 React 前端（JSON 打包进静态文件）
  ↓
Vercel 自动部署
```

## 采集策略（合规）

- ✅ 只访问公开页面，无需登录
- ✅ robots.txt 不存在，无限制规则
- ✅ 截图 + Vision OCR，非结构化爬虫
- ✅ 每次请求间隔 ≥5 秒
- ✅ 只采集必要字段

## 目录结构

```
├── scraper/
│   ├── scrape.py          # 采集脚本（Playwright + Gemini Vision）
│   └── requirements.txt
├── data/
│   └── history/
│       └── data.json      # 历史数据（每日追加）
├── frontend/              # React + Recharts 前端
│   └── src/
│       ├── pages/Dashboard.tsx
│       ├── components/
│       └── utils/data.ts
└── .github/workflows/
    └── daily-scrape.yml   # 每日自动运行
```

## 部署到自己的 GitHub

1. Fork 此仓库
2. 申请百度 OCR API：https://console.bce.baidu.com/ai/#/ai/ocr/overview/index
   - 创建应用后获得 API Key 和 Secret Key
   - 通用文字识别（标准版）每天 **500次免费**，完全够用
3. 在 GitHub Secrets 中添加（仓库 Settings → Secrets → Actions）：
   - `BAIDU_OCR_API_KEY`：百度应用的 API Key
   - `BAIDU_OCR_SECRET_KEY`：百度应用的 Secret Key
4. 开启 GitHub Pages：Settings → Pages → Source 选 `gh-pages` 分支
5. 手动触发一次 workflow 验证：Actions → 每日采集 → Run workflow
6. 之后每天 08:30 自动运行

## OCR 方案说明

截图采集策略（合规）：
- GitHub Actions 在境外服务器运行，可正常渲染 fangdi.com.cn
- Playwright 启动无头 Chromium 截取完整页面截图
- 截图发送给百度 OCR（通用文字识别）提取文字
- 正则表达式从文字中解析套数、面积等数字
- 每次请求间隔 ≥6 秒
